import React,{useEffect,useMemo,useState}from'react';
import{createRoot}from'react-dom/client';
import{BarChart,Bar,XAxis,YAxis,Tooltip,ResponsiveContainer,CartesianGrid}from'recharts';
import'./style.css';

const API=(import.meta as any).env.VITE_API_URL||'http://localhost:8000';
type Tab='Overview'|'Targets'|'Attack Library'|'Run Test'|'Report'|'Live Console';
const tabs:{name:Tab;icon:string;hint:string}[]=[
  {name:'Overview',icon:'⌂',hint:'Security posture'},
  {name:'Targets',icon:'◎',hint:'Connected systems'},
  {name:'Attack Library',icon:'◇',hint:'Adversarial corpus'},
  {name:'Run Test',icon:'▷',hint:'New assessment'},
  {name:'Report',icon:'▥',hint:'Findings & evidence'},
  {name:'Live Console',icon:'⌁',hint:'Inspect messages'},
];
async function api(path:string,opts:any={}){const r=await fetch(API+path,{headers:{'content-type':'application/json'},...opts});if(!r.ok)throw new Error(await r.text());return r.json()}

function App(){
  const[tab,setTab]=useState<Tab>('Overview');const[targets,setTargets]=useState<any[]>([]);const[attacks,setAttacks]=useState<any[]>([]);const[run,setRun]=useState<any>();const[report,setReport]=useState<any>();const[error,setError]=useState('');
  const refresh=()=>Promise.all([api('/targets').then(setTargets),api('/attacks').then(setAttacks)]).catch(e=>setError(e.message));
  useEffect(()=>{refresh()},[]);
  useEffect(()=>{if(!run||run.status==='completed')return;const id=setInterval(()=>api('/tests/'+run.id).then((r:any)=>{setRun(r);if(r.status==='completed')api('/reports/'+r.id).then(x=>{setReport(x);setTab('Report')})}),1000);return()=>clearInterval(id)},[run]);
  const start=async(config:any)=>{setError('');const x=await api('/tests',{method:'POST',body:JSON.stringify(config)});setRun({id:x.test_run_id,status:'queued',executed:0,total:0})};
  return <div className="app-shell"><Sidebar tab={tab} setTab={setTab} attacks={attacks.length}/><div className="workspace"><Topbar tab={tab}/><main>{error&&<div className="error-banner"><b>Connection error</b><span>{error}</span><button onClick={()=>setError('')}>×</button></div>}{tab==='Overview'&&<Overview targets={targets} attacks={attacks} report={report} go={setTab}/>} {tab==='Targets'&&<Targets items={targets} done={refresh}/>} {tab==='Attack Library'&&<Library items={attacks}/>} {tab==='Run Test'&&<Runner targets={targets} attacks={attacks} run={run} start={start}/>} {tab==='Report'&&<Report report={report}/>} {tab==='Live Console'&&<Console targets={targets}/>}</main></div></div>
}

function Sidebar({tab,setTab,attacks}:any){return <aside className="sidebar"><div className="brand"><div className="brand-mark">P</div><div><b>PromptGuard</b><span>AI Red Team</span></div></div><div className="workspace-switch"><span>PG</span><div><small>WORKSPACE</small><b>Security Lab</b></div><i>⌄</i></div><nav>{tabs.map(x=><button key={x.name} className={tab===x.name?'active':''} onClick={()=>setTab(x.name)}><span className="nav-icon">{x.icon}</span><span><b>{x.name}</b><small>{x.hint}</small></span>{x.name==='Attack Library'&&<em>{attacks||'—'}</em>}</button>)}</nav><div className="sidebar-foot"><div className="engine"><i/><span><b>Defense engine</b><small>All systems operational</small></span></div><div className="operator"><span>SP</span><div><b>Security Operator</b><small>Local workspace</small></div><i>•••</i></div></div></aside>}
function Topbar({tab}:any){return <header><div><small>PROMPTGUARD /</small><b>{tab}</b></div><div className="header-actions"><span className="secure"><i/> Engine online</span><button title="Notifications">♢</button><button className="avatar">SP</button></div></header>}

function Overview({targets,attacks,report,go}:any){const cats=new Set(attacks.map((a:any)=>a.category)).size;return <section><div className="hero"><div><span className="eyebrow">AI SECURITY CONTROL CENTER</span><h1>Know how your AI behaves<br/><em>under pressure.</em></h1><p>Continuously test connected assistants against prompt injection, jailbreaks, data leakage, and policy manipulation.</p><div className="hero-actions"><button className="primary" onClick={()=>go('Run Test')}>▷ Start an assessment</button><button className="secondary" onClick={()=>go('Targets')}>Connect a target</button></div></div><div className="readiness"><div className="ring"><span><b>{targets.length?'92':'64'}</b><small>/ 100</small></span></div><b>Workspace readiness</b><small>{targets.length?'Ready for adversarial testing':'Connect a target to get started'}</small></div></div><div className="metric-grid"><Metric icon="◎" label="Connected targets" value={targets.length} foot={targets.length?'Authorized & active':'Awaiting first target'} tone="cyan"/><Metric icon="◇" label="Attack patterns" value={attacks.length} foot={`${cats} security categories`} tone="purple"/><Metric icon="⌁" label="Last risk score" value={report?report.risk_score_overall:'—'} foot={report?'Latest completed assessment':'No completed assessment'} tone="orange"/><Metric icon="✓" label="Engine status" value="Ready" foot="Deterministic detection online" tone="green"/></div><div className="overview-grid"><div className="panel quick"><div className="panel-title"><div><span className="eyebrow">QUICK START</span><h2>Launch your first security test</h2></div><span className="step-count">3 steps</span></div><div className="steps"><Step n="01" title="Connect your chatbot" text="Add the public API URL of an assistant you are authorized to test." done={!!targets.length} action={()=>go('Targets')}/><Step n="02" title="Choose the attack surface" text="Select categories and transformations from the curated corpus." done={false} action={()=>go('Run Test')}/><Step n="03" title="Review actionable evidence" text="See successful attacks, exact payloads, severity and recommended fixes." done={!!report} action={()=>go('Report')}/></div></div><div className="panel coverage"><div className="panel-title"><div><span className="eyebrow">DEFENSE LAYERS</span><h2>Detection pipeline</h2></div><span className="live-dot">LIVE</span></div><div className="layers"><Layer name="Structural rules" value={100}/><Layer name="Encoding analysis" value={100}/><Layer name="Semantic similarity" value={96}/><Layer name="Response analysis" value={92}/></div><p className="note">Each request is evaluated across multiple independent security signals.</p></div></div></section>}
function Metric({icon,label,value,foot,tone}:any){return <div className={'metric '+tone}><span className="metric-icon">{icon}</span><div><small>{label}</small><b>{value}</b><p>{foot}</p></div></div>}
function Step({n,title,text,done,action}:any){return <button className="step" onClick={action}><span className={done?'done':''}>{done?'✓':n}</span><div><b>{title}</b><small>{text}</small></div><i>→</i></button>}
function Layer({name,value}:any){return <div className="layer"><div><b>{name}</b><span>{value}%</span></div><div><i style={{width:value+'%'}}/></div></div>}

function PageHead({eyebrow,title,description,action}:any){return <div className="page-head"><div><span className="eyebrow">{eyebrow}</span><h1>{title}</h1><p>{description}</p></div>{action}</div>}
function Targets({items,done}:any){const[form,setForm]=useState({name:'',api_endpoint:'',authorized:false});const[busy,setBusy]=useState(false);return <section><PageHead eyebrow="TARGET MANAGEMENT" title="Connect your AI system" description="Add any HTTP chatbot you own or have explicit permission to security-test."/><div className="target-layout"><form className="panel connect-card" onSubmit={async e=>{e.preventDefault();setBusy(true);try{await api('/targets',{method:'POST',body:JSON.stringify({...form,canary:'GENESIS-7731-INTERNAL'})});setForm({name:'',api_endpoint:'',authorized:false});await done()}finally{setBusy(false)}}}><div className="form-title"><span>＋</span><div><h2>New target</h2><p>No code required—just enter the chatbot name and API URL.</p></div></div><label>Chatbot name<input required placeholder="e.g. Customer Support Assistant" value={form.name} onChange={e=>setForm({...form,name:e.target.value})}/></label><label>API endpoint<div className="url-input"><span>https://</span><input required placeholder="api.example.com/v1/chat/completions" value={form.api_endpoint.replace(/^https?:\/\//,'')} onChange={e=>setForm({...form,api_endpoint:'https://'+e.target.value})}/></div><small>OpenAI-compatible chat completions endpoints work out of the box.</small></label><label className="check"><input type="checkbox" checked={form.authorized} onChange={e=>setForm({...form,authorized:e.target.checked})}/><span><b>I confirm I am authorized to test this system.</b><small>Only test applications that you own or have written permission to assess.</small></span></label><button className="primary wide" disabled={!form.authorized||busy}>{busy?'Connecting…':'Connect target →'}</button></form><div><div className="list-head"><div><h2>Connected targets</h2><span>{items.length}</span></div><small>{items.length?'Ready for assessment':'No targets connected'}</small></div>{items.length?items.map((x:any)=><article className="panel target-card" key={x.id}><div className="target-avatar">{x.name.slice(0,2).toUpperCase()}</div><div className="target-info"><div><h3>{x.name}</h3><span className="badge green">● AUTHORIZED</span></div><code>{x.api_endpoint}</code><small>OpenAI-compatible HTTP target</small></div><button title="Target options">•••</button></article>):<div className="panel empty compact"><span>◎</span><b>No connected systems yet</b><small>Your authorized targets will appear here.</small></div>}</div></div></section>}

function Library({items}:any){const[q,setQ]=useState('');const[category,setCategory]=useState('All');const cats=useMemo(()=>['All',...new Set(items.map((x:any)=>x.category))],[items]);const visible=items.filter((x:any)=>(category==='All'||x.category===category)&&(x.title+x.category+x.prompt).toLowerCase().includes(q.toLowerCase()));return <section><PageHead eyebrow="ADVERSARIAL INTELLIGENCE" title="Attack library" description="Curated and normalized techniques used to evaluate real-world AI failure modes." action={<div className="head-stat"><b>{items.length}</b><span>total patterns</span></div>}/><div className="library-stats"><div><span>◇</span><b>{cats.length-1}</b><small>attack categories</small></div><div><span>↗</span><b>{items.filter((x:any)=>x.source_severity==='HIGH').length}</b><small>high-severity patterns</small></div><div><span>⌁</span><b>10</b><small>mutation strategies</small></div></div><div className="panel library"><div className="library-tools"><div className="search-wrap"><span>⌕</span><input placeholder="Search title, category, or payload…" value={q} onChange={e=>setQ(e.target.value)}/></div><select value={category} onChange={e=>setCategory(e.target.value)}>{cats.map((c:any)=><option key={c} value={c}>{c==='All'?'All categories':c.replaceAll('_',' ')}</option>)}</select><span className="result-count">{visible.length} results</span></div><div className="attack-table"><div className="attack-row table-head"><span>ID</span><span>Category</span><span>Technique</span><span>Severity</span><span/></div>{visible.map((a:any)=><details key={a.id}><summary className="attack-row"><code>#{String(a.id).padStart(3,'0')}</code><span className="category-tag">{a.category.replaceAll('_',' ')}</span><b>{a.title}</b><span className={'severity '+a.source_severity}>{a.source_severity}</span><i>⌄</i></summary><div className="attack-detail"><div><small>ATTACK PAYLOAD</small><code>{a.prompt}</code></div><div><small>EXPECTED SAFE BEHAVIOUR</small><p>{a.expected_safe_behaviour}</p><small>REMEDIATION</small><p>{a.remediation}</p></div></div></details>)}</div></div></section>}

const MUTATIONS=['base64','hex','leetspeak','unicode_homoglyph','zero_width_insert','roleplay_wrap','delimiter_inject','split_2_turns','translate_hi','markdown_hide'];
function Runner({targets,attacks,run,start}:any){const[id,setId]=useState(targets[0]?.id);const[count,setCount]=useState(20);const[categories,setCategories]=useState<string[]>([]);const[mutations,setMutations]=useState<string[]>(['base64','roleplay_wrap']);const[variants,setVariants]=useState(1);const[judge,setJudge]=useState(false);const[enforce,setEnforce]=useState(false);useEffect(()=>{if(!id&&targets[0])setId(targets[0].id)},[targets]);const cats=[...new Set(attacks.filter((a:any)=>a.origin==='seed').map((a:any)=>a.category))] as string[];const toggle=(list:string[],v:string,setter:any)=>setter(list.includes(v)?list.filter(x=>x!==v):[...list,v]);const estimated=Math.min(count,attacks.filter((a:any)=>a.origin==='seed'&&(!categories.length||categories.includes(a.category))).length)*(1+Math.min(variants,mutations.length));const pct=run?.total?100*run.executed/run.total:0;return <section><PageHead eyebrow="CAMPAIGN BUILDER" title="Configure assessment" description="Build a focused, reproducible adversarial campaign against an authorized target." action={<div className="estimate"><small>ESTIMATED LOAD</small><b>≈ {estimated} requests</b></div>}/><div className="run-layout"><div className="panel runner"><div className="section-number"><span>01</span><div><h3>Target and scale</h3><p>Choose the system and assessment depth.</p></div></div><div className="form-row"><label>Target<select value={id||''} onChange={e=>setId(+e.target.value)}><option value="" disabled>Select a connected target</option>{targets.map((x:any)=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><label>Base attacks<input type="number" min="1" max="500" value={count} onChange={e=>setCount(+e.target.value)}/></label><label>Variants / attack<input type="number" min="0" max="10" value={variants} onChange={e=>setVariants(+e.target.value)}/></label></div><div className="divider"/><div className="section-number"><span>02</span><div><h3>Attack surface</h3><p>Select categories or leave blank to test everything.</p></div></div><div className="chips">{cats.map(c=><button type="button" key={c} className={categories.includes(c)?'selected':''} onClick={()=>toggle(categories,c,setCategories)}>{categories.includes(c)?'✓ ':''}{c.replaceAll('_',' ')}</button>)}</div><div className="divider"/><div className="section-number"><span>03</span><div><h3>Evasion transformations</h3><p>Stress-test defenses against encoded and obfuscated variants.</p></div></div><div className="chips mutations">{MUTATIONS.map(m=><button type="button" key={m} className={mutations.includes(m)?'selected':''} onClick={()=>toggle(mutations,m,setMutations)}>{mutations.includes(m)?'✓ ':''}{m.replaceAll('_',' ')}</button>)}</div><div className="toggles"><Toggle value={judge} set={setJudge} title="Semantic judge" text="Add model-assisted response analysis"/><Toggle value={enforce} set={setEnforce} title="Request blocking" text="Stop detected attacks before target"/></div><button className="primary launch" onClick={()=>start({target_id:id,count,categories,mutations,variants_per_attack:variants,judge_enabled:judge,enforce_request_block:enforce})} disabled={!id||run?.status==='running'}>{run?.status==='running'?'Assessment running…':'▷ Launch assessment'}</button>{run&&<div className="progress"><div><b>{run.status.toUpperCase()}</b><span>{run.executed}/{run.total||'preparing'}</span></div><div className="progress-bar"><i style={{width:pct+'%'}}/></div><small>{Math.round(pct)}% complete</small></div>}</div><aside className="panel campaign-summary"><span className="eyebrow">CAMPAIGN SUMMARY</span><h2>Ready to launch</h2><dl><Summary label="Target" value={targets.find((x:any)=>x.id===id)?.name||'Not selected'}/><Summary label="Attack coverage" value={`${categories.length||cats.length} categories`}/><Summary label="Transformations" value={`${mutations.length} selected`}/><Summary label="Request gate" value={enforce?'Enforced':'Observe only'}/><Summary label="Response judge" value={judge?'Semantic + rules':'Deterministic'}/></dl><div className="summary-note"><span>i</span><p>Observe-only mode lets attacks reach the target so PromptGuard can measure its native resistance.</p></div></aside></div></section>}
function Toggle({value,set,title,text}:any){return <label className="toggle"><input type="checkbox" checked={value} onChange={e=>set(e.target.checked)}/><span/><div><b>{title}</b><small>{text}</small></div></label>}
function Summary({label,value}:any){return <div><dt>{label}</dt><dd>{value}</dd></div>}

function Report({report}:any){if(!report)return <section><PageHead eyebrow="ASSESSMENT INTELLIGENCE" title="Security report" description="Evidence, risk scoring, and remediation will appear after an assessment."/><div className="panel empty report-empty"><div className="radar">⌁</div><h2>No report generated yet</h2><p>Run an adversarial campaign to see which attacks succeeded and how to fix them.</p></div></section>;return <section><PageHead eyebrow="ASSESSMENT INTELLIGENCE" title="Security assessment" description={report.target_name} action={<a className="secondary" href={API+'/reports/'+report.run_id+'?format=md'}>↓ Export report</a>}/><div className="risk-banner"><div className="risk-score"><small>OVERALL RISK</small><b>{report.risk_score_overall}</b><span>/ 100</span></div><div><h2>{report.risk_score_overall>=70?'Critical exposure detected':report.risk_score_overall>=40?'Security hardening required':'Strong resistance observed'}</h2><p>Review successful attacks below and apply the recommended controls.</p></div></div><div className="kpis">{Object.entries(report.totals).slice(0,5).map(([k,v])=><K key={k} label={k} v={v}/>)}</div><div className="panel chart"><div className="panel-title"><div><span className="eyebrow">RESILIENCE BY CATEGORY</span><h2>Attack outcomes</h2></div><div className="legend"><span className="bad">● Successful</span><span className="good">● Resisted</span></div></div><ResponsiveContainer width="100%" height={280}><BarChart data={report.by_category}><CartesianGrid stroke="#24344b" vertical={false}/><XAxis dataKey="category" tick={{fill:'#7d8da6',fontSize:10}} tickFormatter={(x:string)=>x.replaceAll('_',' ')}/><YAxis tick={{fill:'#7d8da6'}}/><Tooltip contentStyle={{background:'#111c2c',border:'1px solid #293b55',borderRadius:10}}/><Bar dataKey="successful" fill="#ff6577" radius={[5,5,0,0]}/><Bar dataKey="resisted" fill="#39d6a0" radius={[5,5,0,0]}/></BarChart></ResponsiveContainer></div><div className="findings-head"><div><h2>Detailed findings</h2><p>Every result includes the exact evidence and recommended mitigation.</p></div><span>{report.findings.length} findings</span></div>{report.findings.map((f:any)=><details key={f.execution_id} className={'finding '+f.outcome}><summary><span className={'outcome '+f.outcome}>{f.outcome==='SUCCESSFUL'?'!':'✓'}</span><div><b>{f.title}</b><small>{f.category.replaceAll('_',' ')}</small></div><span className={'severity '+f.derived_severity}>{f.derived_severity}</span><i>⌄</i></summary><div className="finding-detail"><div><small>PAYLOAD SENT</small><code>{f.payload_used}</code><small>TARGET RESPONSE</small><code>{f.response_excerpt}</code></div><div><small>REQUEST PIPELINE</small><p>Verdict: <b>{f.request_verdict}</b><br/>Reached target: <b>{String(f.reached_target)}</b></p><div className="fix"><small>RECOMMENDED FIX</small><p>{f.remediation}</p></div></div></div></details>)}</section>}
function K({label,v}:any){return <div className="panel kpi"><small>{label.replaceAll('_',' ')}</small><b>{String(v)}</b></div>}

function Console({targets}:any){const[id,setId]=useState(targets[0]?.id);const[msg,setMsg]=useState('');const[result,setResult]=useState<any>();const[busy,setBusy]=useState(false);useEffect(()=>{if(!id&&targets[0])setId(targets[0].id)},[targets]);const send=async()=>{setBusy(true);try{setResult(await api('/proxy/chat',{method:'POST',body:JSON.stringify({target_id:id,session_id:'ui-live',message:msg})}))}finally{setBusy(false)}};return <section><PageHead eyebrow="REAL-TIME ANALYSIS" title="Live inspection console" description="Send one message through the complete PromptGuard detection pipeline."/><div className="console-layout"><div className="panel console"><div className="console-top"><label>Active target<select value={id||''} onChange={e=>setId(+e.target.value)}><option value="" disabled>Select target</option>{targets.map((x:any)=><option key={x.id} value={x.id}>{x.name}</option>)}</select></label><span><i/> Protected session</span></div><div className="conversation"><div className="system-message"><span>PG</span><p>Enter a normal user request or a suspected injection payload. I’ll trace how every defense layer handles it.</p></div>{msg&&result&&<div className="user-message"><p>{msg}</p><span>YOU</span></div>}</div><textarea placeholder="Type a message or paste a prompt injection attempt…" value={msg} onChange={e=>setMsg(e.target.value)}/><div className="send-row"><small>{msg.length} characters</small><button className="primary" disabled={!id||!msg||busy} onClick={send}>{busy?'Inspecting…':'Inspect & send →'}</button></div>{result&&<div className="console-result"><div className="verdict"><span className={result.request_verdict.action==='ALLOW'?'allow':'block'}>{result.request_verdict.action}</span><b>Pipeline verdict</b></div><pre>{result.response||result.notice}</pre></div>}</div><aside className="panel pipeline-card"><span className="eyebrow">DETECTION PIPELINE</span><h2>Message trace</h2>{['Input normalization','Encoding analysis','Pattern detection','Semantic similarity','Policy decision','Target response'].map((x,i)=><div className={'pipe-step '+(result?'complete':'')} key={x}><span>{result?'✓':i+1}</span><div><b>{x}</b><small>{result?'Completed':'Waiting for input'}</small></div></div>)}</aside></div></section>}

createRoot(document.getElementById('root')!).render(<App/>);
